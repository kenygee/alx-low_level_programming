#include "main.h"
#include <string.h>

/**
 * _strcmp -> a function that compare two string
 * @s1: string one
 * @s2: string two
 * Return: string
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
