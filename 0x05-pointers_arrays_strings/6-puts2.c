#include "main.h"

/**
 * puts2 -> a function that prints every other character of a string
 * @str: parameter string
 */

void puts2(char *str)
{
	char *str;

	str = "0123456789";
	puts2(str);
	return (0);
}
