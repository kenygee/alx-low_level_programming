#include "main.h"

/**
 *_isdigit - check whether a charater is a digit or not
 *@c: tested charater c
 *Return: 1 if c is digit otherwish 0
 */

int _isdigit(int c)
{
	if ((c >= 48) && (c <= 57))
	{
	return (0);
	}
}
