#include <main.h>

/**
 *_isupper - it check for upper case
 *@c: the charater to be tested
 *Return: 1 whether it is or 0 if is not
 */

int _isupper(int c)

{
	if ((c >= 'A') && (c <= 'B'))
	{
	return (1);
	}
}
