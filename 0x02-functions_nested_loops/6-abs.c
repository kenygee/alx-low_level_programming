#include "main.h"

/**
 * _abs:it compute the absolute value of integer
 * @n: delcares the integer
 * Return: integer if it fulfill every condition
 */

int _abs(n)
{
if (n < 0)
	n = -n;
	return (n);
}
