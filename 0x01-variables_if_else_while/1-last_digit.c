#include <stdio.h>
#include <time.h>

/**
 * main - Prints the alphabeticaly
 * Return: Always (Success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)

		putchar(c);

	putchar('\n');

	return (0);
}
